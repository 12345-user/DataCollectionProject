```mermaid
flowchart TD
    A[Step01 数据采集] --> B[Step02 扩展候选论文]
    B --> C[Step03 作者消歧]
    C --> D[Step04 关键词/实体与质量评分]
    D --> E[Step05 领域聚类与主题命名]
    E --> F[Step06 时间分析与投入预测]
    F --> G[Step07 DuckDB + Streamlit可视化]
```

```mermaid
flowchart TD
    A[输入: 教授名 + 种子论文标题/PMID] --> B[调用 run_professor_collection_simple.ps1]
    B --> C[PubMed E-utilities 拉取论文元数据]
    C --> D[解析标题/摘要/作者/DOI/期刊/发布时间]
    D --> E[生成 abstracts.jsonl 与摘要清单]
    E --> F[生成 lab_info.json]
```

```mermaid
flowchart TD
    A[输入: Step01 abstracts + 教授名] --> B[读取本地/远程来源]
    B --> C[PubMed 扩展]
    B --> D[arXiv/bioRxiv/medRxiv/chemRxiv 扩展]
    B --> E[OpenAIRE 扩展]
    C --> F[统一字段映射]
    D --> F
    E --> F
    F --> G[去重: PMID/DOI/标准化标题]
    G --> H[输出 expanded_papers.jsonl]
```

```mermaid
flowchart TD
    A[输入: Step02 expanded_papers + Step01 lab_info] --> B[姓名匹配候选]
    B --> C[机构/单位关键词匹配]
    C --> D[计算 identity_score]
    D --> E[identity_decision true/false]
    E --> F[输出 step03_disambiguated_papers.jsonl]
```

```mermaid
flowchart TD
    A[输入: Step03 消歧后论文] --> B[文本预处理 title+abstract]
    B --> C[KeyBERT 提取关键词]
    B --> D[实体识别(GLiNER/规则)]
    C --> E[项目候选 project_candidates]
    D --> E
    E --> F[质量评分 quality_score 计算]
    F --> G[输出 step04_keyword_entity.jsonl]
```

```mermaid
flowchart TD
    A[输入: Step04 关键词实体结果] --> B[构建聚类文本 title+abstract+keywords]
    B --> C[sentence-transformers 向量化]
    C --> D[HDBSCAN 聚类]
    D --> E{簇数量是否足够}
    E -- 否 --> F[KMeans 保底生成最少领域数]
    E -- 是 --> G[保留当前簇]
    F --> H[统计领域关键词]
    G --> H
    H --> I[中文主题命名 domain_theme_cn]
    I --> J[输出 per-paper + per-domain]
```

```mermaid
flowchart TD
    A[输入: Step05 paper_domains] --> B[按年份聚合 paper_count/weight]
    B --> C[计算当前领域投入占比]
    B --> D[构建历史时间序列]
    D --> E[预测模型: Linear/ETS/ARIMA/Auto]
    E --> F[回测 MAE/MAPE 选模]
    C --> G[输出 allocation]
    D --> H[输出 timeline]
    E --> I[输出 trend_report]
```

```mermaid
flowchart TD
    A[输入: Step05 + Step06 结果] --> B[build_step07_duckdb.py 写入 DuckDB]
    B --> C[生成视图 v_domain_share / v_domain_time]
    C --> D[Streamlit 读取 DuckDB]
    D --> E[图1: 领域占比饼图]
    D --> F[图2: 时间频率折线]
    D --> G[图3: 权重折线]
    D --> H[公式说明/时间核验/论文清单]
```

